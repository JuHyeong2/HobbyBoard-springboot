<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hamo.board.model.mapper.BoardMapper">
	<select id="selectBoardList" resultType="Board">
		select *
		from board b
			join member m on b.member_no = m.member_no
			join location l on b.location_no = l.location_no
			join category c on b.category_no = c.category_no
		where b.board_status = 'Y'
		order by b.board_no DESC
	</select>
	
	<select id="selectBoard">
		select *
		from board b
			join member m on b.member_no = m.member_no
			join location l on b.location_no = l.location_no
			join category c on b.category_no = c.category_no
		where board_no = #{boardNo}
	</select>
	
<<<<<<< HEAD
	<insert id="boardInsert">
		insert into board
		values(seq_board.nextval, #{title}, #{content}, default, default, default, #{maxParticipants}, default, #{memberNo}, #{locationNo}, #{categoryNo})
	</insert>
	
=======
	<select id="selectNotice" resultType="Admin">
		select *
		from admin_board
		where admin_board_status='Y' and
			  board_no=#{boardNo}		  
	</select>
	<update id="updateNoticeCount">
		update admin_board
		set views = views+1
		where board_no=#{boardNo}
	</update>
>>>>>>> 1d8a7baad3fa21d37c3ff3e345117920d381b651
</mapper>